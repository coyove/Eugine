(struct Config "Configurations"
	[::port := 8080]
	[::socket-read-timeout := 5000]
	[::site-name := "Test Site"]
	[::salt := "test salt"]

	[::posts-per-page := 20]

	[::new-member-karma := 10]
	[::new-member-group := "user"]

	[::cookie-options := (dict ("Expires" 31536000))]
)